idosos_sedentarios <-
data.frame(sexo = sample(sexo, 100, replace = T),
atividade_fisica = "Sedentario",
idade = round(rnorm(100, 60, 5)),
bpm = round(rnorm(100, 55, 7)))
dados <- rbind(adultos_ativos,
adultos_sedentarios,
idosos_ativos,
idosos_sedentarios)
dados
dados <- sample(rbind(adultos_ativos,
adultos_sedentarios,
idosos_ativos,
idosos_sedentarios))
dados
adultos_ativos <-
data.frame(sexo = sample(sexo, 100, replace = T),
atividade_fisica = "Ativo",
idade = round(rnorm(100, 35, 5)),
bpm = round(rnorm(100, 55, 7)))
adultos_sedentarios <-
data.frame(sexo = sample(sexo, 100, replace = T),
atividade_fisica = "Sedentario",
idade = round(rnorm(100, 35, 5)),
bpm = round(rnorm(100, 75, 7)))
idosos_ativos <-
data.frame(sexo = sample(sexo, 100, replace = T),
atividade_fisica = "Ativo",
idade = round(rnorm(100, 60, 5)),
bpm = round(rnorm(100, 75, 7)))
idosos_sedentarios <-
data.frame(sexo = sample(sexo, 100, replace = T),
atividade_fisica = "Sedentario",
idade = round(rnorm(100, 60, 5)),
bpm = round(rnorm(100, 55, 7)))
dados <- sample(rbind(adultos_ativos,
adultos_sedentarios,
idosos_ativos,
idosos_sedentarios))
# Qual é a média geral de batimentos cardíacos?
mean(dados$bpm)
# Qual é o tamanho da amostra?
nrow(dados)
# A amostra é composta por quantos indivíduos do sexo masculino?
table(dados$sexo)
# A amostra é composta por quantos indivíduos do sexo masculino?
table(dados$sexo)[2]
# Qual é a proporção de indivíduos sedentários da amostra?
table(dados$atividade_fisica)
# Qual é a proporção de indivíduos sedentários da amostra?
prop.table(table(dados$atividade_fisica))
# Qual é a proporção de indivíduos sedentários da amostra?
prop.table(table(dados$atividade_fisica))[2]
# Qual é a proporção de indivíduos sedentários da amostra?
prop.table(table(dados$atividade_fisica))[2]*100
# Qual é a média geral de batimentos cardíacos?
mean(dados$bpm)
# Qual é o tamanho da amostra?
nrow(dados)
# A amostra é composta por quantos indivíduos do sexo masculino?
table(dados$sexo)[2]
# Qual é o percentual de indivíduos sedentários da amostra?
prop.table(table(dados$atividade_fisica))[2]*100
adultos_ativos <-
data.frame(sexo = sample(sexo, 100, replace = T),
atividade_fisica = "Ativo",
idade = round(rnorm(100, 35, 5)),
bpm = round(rnorm(100, 55, 7)))
adultos_sedentarios <-
data.frame(sexo = sample(sexo, 100, replace = T),
atividade_fisica = "Sedentario",
idade = round(rnorm(100, 35, 5)),
bpm = round(rnorm(100, 75, 7)))
idosos_ativos <-
data.frame(sexo = sample(sexo, 100, replace = T),
atividade_fisica = "Ativo",
idade = round(rnorm(100, 60, 5)),
bpm = round(rnorm(100, 75, 7)))
idosos_sedentarios <-
data.frame(sexo = sample(sexo, 100, replace = T),
atividade_fisica = "Sedentario",
idade = round(rnorm(100, 60, 5)),
bpm = round(rnorm(100, 55, 7)))
dados <- sample(rbind(adultos_ativos,
adultos_sedentarios,
idosos_ativos,
idosos_sedentarios))
# Qual é a média geral de batimentos cardíacos?
mean(dados$bpm)
# Qual é o tamanho da amostra?
nrow(dados)
# A amostra é composta por quantos indivíduos do sexo masculino?
table(dados$sexo)[2]
# Qual é o percentual de indivíduos sedentários da amostra?
prop.table(table(dados$atividade_fisica))[2]*100
#
head(dados)
# Qual é a média de batimentos cardíacos para indivíduos do sexo
# masculino
tapply(X = dados$bpm,
INDEX = dados$sexo,
FUN = mean)
# Qual é a média de batimentos cardíacos para indivíduos do sexo
# masculino
tapply(X = dados$bpm,
INDEX = dados$sexo,
FUN = mean)[2]
# Qual é a média de batimentos cardíacos para indivíduos sedentários
tapply(X = dados$bpm,
INDEX = dados$atividade_fisica,
FUN = mean)[2]
# Qual é a média de batimentos cardíacos para indivíduos sedentários
tapply(X = dados$bpm,
INDEX = dados$atividade_fisica,
FUN = mean)
# Qual é a idade mediana?
median(dados$idade)
adultos_ativos <-
data.frame(sexo = sample(sexo, 100, replace = T),
atividade_fisica = "Ativo",
idade = round(rnorm(100, 35, 5)),
bpm = round(rnorm(100, 55, 7)))
adultos_sedentarios <-
data.frame(sexo = sample(sexo, 100, replace = T),
atividade_fisica = "Sedentario",
idade = round(rnorm(100, 35, 5)),
bpm = round(rnorm(100, 75, 7)))
idosos_ativos <-
data.frame(sexo = sample(sexo, 100, replace = T),
atividade_fisica = "Ativo",
idade = round(rnorm(100, 60, 5)),
bpm = round(rnorm(100, 75, 7)))
idosos_sedentarios <-
data.frame(sexo = sample(sexo, 100, replace = T),
atividade_fisica = "Sedentario",
idade = round(rnorm(100, 60, 5)),
bpm = round(rnorm(100, 55, 7)))
dados <- sample(rbind(adultos_ativos,
adultos_sedentarios,
idosos_ativos,
idosos_sedentarios))
# Qual é o tamanho da amostra?
nrow(dados)
# A amostra é composta por quantos indivíduos do sexo masculino?
table(dados$sexo)[2]
# Qual é o percentual de indivíduos sedentários da amostra?
prop.table(table(dados$atividade_fisica))[2]*100
# Qual é a média geral de batimentos cardíacos?
mean(dados$bpm)
# Qual é a média de batimentos cardíacos para indivíduos do sexo masculino
tapply(X = dados$bpm,
INDEX = dados$sexo,
FUN = mean)[2]
# Qual é a média de batimentos cardíacos para indivíduos sedentários
tapply(X = dados$bpm,
INDEX = dados$atividade_fisica,
FUN = mean)[2]
# Qual é a idade mediana da amostra?
median(dados$idade)
tapply(X = dados$bpm,
INDEX = dados$atividade_fisica,
FUN = quantile)
# Qual é o primeiro quartil de batimentos cardíacos para indivíduos ativos?
tapply(X = dados$bpm,
INDEX = dados$atividade_fisica,
FUN = quantile)[2]
# Qual é o primeiro quartil de batimentos cardíacos para indivíduos ativos?
tapply(X = dados$bpm,
INDEX = dados$atividade_fisica,
FUN = quantile)
# Qual é o primeiro quartil de batimentos cardíacos para indivíduos ativos?
tapply(X = dados$bpm,
INDEX = dados$atividade_fisica,
FUN = quantile)$Ativo
# Qual é o primeiro quartil de batimentos cardíacos para indivíduos ativos?
tapply(X = dados$bpm,
INDEX = dados$atividade_fisica,
FUN = quantile)$Ativo[2]
# Qual é o desvio padrão dos batimentos para indivíduos do sexo feminino?
tapply(X = dados$bpm,
INDEX = dados$sexo,
FUN = mean)[2]
# Qual é o desvio padrão dos batimentos para indivíduos do sexo feminino?
tapply(X = dados$bpm,
INDEX = dados$sexo,
FUN = mean)[1]
# Qual é o desvio padrão dos batimentos para indivíduos do sexo feminino?
tapply(X = dados$bpm,
INDEX = dados$sexo,
FUN = sd)[1]
sd(dados$idade)
# Qual é o coeficiente de variação (em percentual) da idade?
(sd(dados$idade)/mean(dados$idade))*100
# Qual é o coeficiente de correlação de Pearson para idade e batimentos?
cor(dados$bpm, dados$idade)
names(dados)
boxplot(bom~sexo, dados)
boxplot(bpm~sexo, dados)
names(dados)
boxplot(bpm~atividade_fisica, dados)
boxplot(bpm~sexo, dados)
boxplot(bpm~atividade_fisica, dados)
boxplot(bpm~idade, dados)
plot(bpm~idade, dados)
boxplot(bpm~sexo, dados)
boxplot(bpm~atividade_fisica, dados)
plot(bpm~idade, dados)
adultos_ativos <-
data.frame(sexo = sample(sexo, 100, replace = T),
atividade_fisica = "Ativo",
idade = round(rnorm(100, 35, 5)),
bpm = round(rnorm(100, 55, 3)))
adultos_sedentarios <-
data.frame(sexo = sample(sexo, 100, replace = T),
atividade_fisica = "Sedentario",
idade = round(rnorm(100, 35, 5)),
bpm = round(rnorm(100, 75, 3)))
idosos_ativos <-
data.frame(sexo = sample(sexo, 100, replace = T),
atividade_fisica = "Ativo",
idade = round(rnorm(100, 60, 5)),
bpm = round(rnorm(100, 75, 3)))
idosos_sedentarios <-
data.frame(sexo = sample(sexo, 100, replace = T),
atividade_fisica = "Sedentario",
idade = round(rnorm(100, 60, 5)),
bpm = round(rnorm(100, 55, 3)))
dados <- sample(rbind(adultos_ativos,
adultos_sedentarios,
idosos_ativos,
idosos_sedentarios))
## Qual é o tamanho da amostra?
nrow(dados)
## A amostra é composta por quantos indivíduos do sexo masculino?
table(dados$sexo)[2]
## Qual é o percentual de indivíduos sedentários da amostra?
prop.table(table(dados$atividade_fisica))[2]*100
## Qual é a média geral de batimentos cardíacos?
mean(dados$bpm)
## Qual é a média de batimentos cardíacos para indivíduos do sexo masculino
tapply(X = dados$bpm,
INDEX = dados$sexo,
FUN = mean)[2]
## Qual é a média de batimentos cardíacos para indivíduos sedentários
tapply(X = dados$bpm,
INDEX = dados$atividade_fisica,
FUN = mean)[2]
## Qual é a idade mediana da amostra?
median(dados$idade)
## Qual é o primeiro quartil de batimentos cardíacos para indivíduos ativos?
tapply(X = dados$bpm,
INDEX = dados$atividade_fisica,
FUN = quantile)$Ativo[2]
# Qual é o desvio padrão dos batimentos para indivíduos do sexo feminino?
tapply(X = dados$bpm,
INDEX = dados$sexo,
FUN = sd)[1]
# Qual é o coeficiente de variação (em percentual) da idade?
(sd(dados$idade)/mean(dados$idade))*100
# Qual é o coeficiente de correlação de Pearson para idade e batimentos?
cor(dados$bpm, dados$idade)
boxplot(bpm~sexo, dados)
boxplot(bpm~atividade_fisica, dados)
plot(bpm~idade, dados)
adultos_ativos <-
data.frame(sexo = sample(sexo, 100, replace = T),
atividade_fisica = "Ativo",
idade = round(rnorm(100, 35, 5)),
bpm = round(rnorm(100, 55, 3)))
adultos_sedentarios <-
data.frame(sexo = sample(sexo, 100, replace = T),
atividade_fisica = "Sedentario",
idade = round(rnorm(100, 35, 5)),
bpm = round(rnorm(100, 75, 3)))
idosos_ativos <-
data.frame(sexo = sample(sexo, 100, replace = T),
atividade_fisica = "Ativo",
idade = round(rnorm(100, 60, 5)),
bpm = round(rnorm(100, 75, 3)))
idosos_sedentarios <-
data.frame(sexo = sample(sexo, 100, replace = T),
atividade_fisica = "Sedentario",
idade = round(rnorm(100, 60, 5)),
bpm = round(rnorm(100, 55, 3)))
dados <- sample(rbind(adultos_ativos,
adultos_sedentarios,
idosos_ativos,
idosos_sedentarios))
## Qual é o tamanho da amostra?
nrow(dados)
## A amostra é composta por quantos indivíduos do sexo masculino?
table(dados$sexo)[2]
## Qual é o percentual de indivíduos sedentários da amostra?
prop.table(table(dados$atividade_fisica))[2]*100
## Qual é a média geral de batimentos cardíacos?
mean(dados$bpm)
## Qual é a média de batimentos cardíacos para indivíduos do sexo masculino
tapply(X = dados$bpm,
INDEX = dados$sexo,
FUN = mean)[2]
## Qual é a média de batimentos cardíacos para indivíduos sedentários
tapply(X = dados$bpm,
INDEX = dados$atividade_fisica,
FUN = mean)[2]
## Qual é a idade mediana da amostra?
median(dados$idade)
## Qual é o primeiro quartil de batimentos cardíacos para indivíduos ativos?
tapply(X = dados$bpm,
INDEX = dados$atividade_fisica,
FUN = quantile)$Ativo[2]
# Qual é o desvio padrão dos batimentos para indivíduos do sexo feminino?
tapply(X = dados$bpm,
INDEX = dados$sexo,
FUN = sd)[1]
# Qual é o coeficiente de variação (em percentual) da idade?
(sd(dados$idade)/mean(dados$idade))*100
# Qual é o coeficiente de correlação de Pearson para idade e batimentos?
cor(dados$bpm, dados$idade)
boxplot(bpm~sexo, dados)
boxplot(bpm~atividade_fisica, dados)
plot(bpm~idade, dados)
boxplot(bpm~sexo, dados)
boxplot(bpm~atividade_fisica, dados)
plot(dados)
plot(bpm~idade, col = atividade_fisica, dados)
boxplot(bpm~sexo, dados)
boxplot(bpm~atividade_fisica, dados)
plot(bpm~idade, col = atividade_fisica, dados)
plot(bpm~idade, dados)
tapply(X = dados$bpm,
INDEX = dados$sexo,
FUN = boxplot())
FUN = boxplot
tapply(X = dados$bpm,
tapply(X = dados$bpm,
INDEX = dados$sexo,
FUN = boxplot)
)
tapply(X = dados$bpm,
INDEX = dados$sexo,
FUN = boxplot)
par(mfrow = c(1,2))
tapply(X = dados$bpm,
INDEX = dados$sexo,
FUN = boxplot)
par(mfrow = c(1,2))
tapply(X = dados$bpm,
INDEX = dados$atividade_fisica,
FUN = boxplot)
adultos_sedentarios)
adultos_sedentarios)
dados <- sample(rbind(adultos_ativos,
adultos_sedentarios))
adultos_ativos <-
data.frame(sexo = sample(sexo, 100, replace = T),
atividade_fisica = "Ativo",
idade = round(rnorm(100, 35, 5)),
bpm = round(rnorm(100, 55, 3)))
adultos_sedentarios <-
data.frame(sexo = sample(sexo, 100, replace = T),
atividade_fisica = "Sedentario",
idade = round(rnorm(100, 35, 5)),
bpm = round(rnorm(100, 75, 3)))
dados <- sample(rbind(adultos_ativos,
adultos_sedentarios))
## Qual é o tamanho da amostra?
nrow(dados)
## A amostra é composta por quantos indivíduos do sexo masculino?
table(dados$sexo)[2]
## Qual é o percentual de indivíduos sedentários da amostra?
prop.table(table(dados$atividade_fisica))[2]*100
## Qual é a média geral de batimentos cardíacos?
mean(dados$bpm)
## Qual é a média de batimentos cardíacos para indivíduos do sexo masculino
tapply(X = dados$bpm,
INDEX = dados$sexo,
FUN = mean)[2]
## Qual é a média de batimentos cardíacos para indivíduos sedentários
tapply(X = dados$bpm,
INDEX = dados$atividade_fisica,
FUN = mean)[2]
## Qual é a idade mediana da amostra?
median(dados$idade)
## Qual é o primeiro quartil de batimentos cardíacos para indivíduos ativos?
tapply(X = dados$bpm,
INDEX = dados$atividade_fisica,
FUN = quantile)$Ativo[2]
# Qual é o desvio padrão dos batimentos para indivíduos do sexo feminino?
tapply(X = dados$bpm,
INDEX = dados$sexo,
FUN = sd)[1]
# Qual é o coeficiente de variação (em percentual) da idade?
(sd(dados$idade)/mean(dados$idade))*100
# Qual é o coeficiente de correlação de Pearson para idade e batimentos?
cor(dados$bpm, dados$idade)
names(dados)
boxplot(bpm~sexo, dados)
boxplot(bpm~atividade_fisica, dados)
plot(bpm~idade, dados)
adultos_ativos <-
data.frame(sexo = sample(sexo, 100, replace = T),
atividade_fisica = "Ativo",
idade = round(rnorm(100, 35, 5)),
bpm = round(rnorm(100, 55, 5)))
adultos_sedentarios <-
data.frame(sexo = sample(sexo, 100, replace = T),
atividade_fisica = "Sedentario",
idade = round(rnorm(100, 35, 5)),
bpm = round(rnorm(100, 75, 5)))
dados <- sample(rbind(adultos_ativos,
adultos_sedentarios))
## Qual é o tamanho da amostra?
nrow(dados)
## A amostra é composta por quantos indivíduos do sexo masculino?
table(dados$sexo)[2]
## Qual é o percentual de indivíduos sedentários da amostra?
prop.table(table(dados$atividade_fisica))[2]*100
## Qual é a média geral de batimentos cardíacos?
mean(dados$bpm)
## Qual é a média de batimentos cardíacos para indivíduos do sexo masculino
tapply(X = dados$bpm,
INDEX = dados$sexo,
FUN = mean)[2]
## Qual é a média de batimentos cardíacos para indivíduos sedentários
tapply(X = dados$bpm,
INDEX = dados$atividade_fisica,
FUN = mean)[2]
## Qual é a idade mediana da amostra?
median(dados$idade)
## Qual é o primeiro quartil de batimentos cardíacos para indivíduos ativos?
tapply(X = dados$bpm,
INDEX = dados$atividade_fisica,
FUN = quantile)$Ativo[2]
# Qual é o desvio padrão dos batimentos para indivíduos do sexo feminino?
tapply(X = dados$bpm,
INDEX = dados$sexo,
FUN = sd)[1]
# Qual é o coeficiente de variação (em percentual) da idade?
(sd(dados$idade)/mean(dados$idade))*100
# Qual é o coeficiente de correlação de Pearson para idade e batimentos?
cor(dados$bpm, dados$idade)
cor(dados$bpm, dados$idade)
# Qual é o coeficiente de correlação de Pearson para idade e batimentos?
cor(dados$bpm, dados$idade)
boxplot(bpm~sexo, dados)
boxplot(bpm~atividade_fisica, dados)
plot(bpm~idade, dados)
par(mfrow = c(1,2))
tapply(X = dados$bpm,
INDEX = dados$atividade_fisica,
FUN = boxplot)
dados <- sample(
rbind(
data.frame(sexo = sample(sexo, 100, replace = T),
atividade_fisica = "Ativo",
idade = round(rnorm(100, 35, 5)),
bpm = round(rnorm(100, 55, 5))),
data.frame(sexo = sample(sexo, 100, replace = T),
atividade_fisica = "Sedentario",
idade = round(rnorm(100, 35, 5)),
bpm = round(rnorm(100, 75, 5)))
)
)
## Qual é o tamanho da amostra?
nrow(dados)
dados <- sample(
rbind(
data.frame(sexo = sample(sexo, 100, replace = T),
atividade_fisica = "Ativo",
idade = round(rnorm(100, 35, 5)),
bpm = round(rnorm(100, 55, 5))),
data.frame(sexo = sample(sexo, 100, replace = T),
atividade_fisica = "Sedentario",
idade = round(rnorm(100, 35, 5)),
bpm = round(rnorm(100, 75, 5)))
)
)
## Qual é o tamanho da amostra?
nrow(dados)
## A amostra é composta por quantos indivíduos do sexo masculino?
table(dados$sexo)[2]
## Qual é o percentual de indivíduos sedentários da amostra?
prop.table(table(dados$atividade_fisica))[2]*100
## Qual é a média geral de batimentos cardíacos?
mean(dados$bpm)
## Qual é a média de batimentos cardíacos para indivíduos do sexo masculino
tapply(X = dados$bpm,
INDEX = dados$sexo,
FUN = mean)[2]
## Qual é a média de batimentos cardíacos para indivíduos sedentários
tapply(X = dados$bpm,
INDEX = dados$atividade_fisica,
FUN = mean)[2]
## Qual é a idade mediana da amostra?
median(dados$idade)
## Qual é o primeiro quartil de batimentos cardíacos para indivíduos ativos?
tapply(X = dados$bpm,
INDEX = dados$atividade_fisica,
FUN = quantile)$Ativo[2]
# Qual é o desvio padrão dos batimentos para indivíduos do sexo feminino?
tapply(X = dados$bpm,
INDEX = dados$sexo,
FUN = sd)[1]
# Qual é o coeficiente de variação (em percentual) da idade?
(sd(dados$idade)/mean(dados$idade))*100
# Qual é o coeficiente de correlação de Pearson para idade e batimentos?
cor(dados$bpm, dados$idade)
plot(bpm~idade)
plot(bpm~idade, dados)
boxplot(bpm~sexo, dados)
boxplot(bpm~atividade_fisica, dados)
write.csv(dados, "dados.csv")
